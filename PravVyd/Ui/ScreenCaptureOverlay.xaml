<Window x:Class="PravVyd.Ui.ScreenCaptureOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="#40000000"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        Cursor="Cross">
    <Canvas x:Name="Root">
        <Rectangle x:Name="Selection"
                   Stroke="#FF4C8DFF"
                   StrokeThickness="2"
                   Fill="#280078D7"
                   Visibility="Collapsed" />
        <Border x:Name="SizeBadge"
                Background="#CC1E1E28"
                CornerRadius="4"
                Padding="6,2"
                Visibility="Collapsed">
            <TextBlock x:Name="SizeText" Foreground="White" FontSize="12" FontFamily="Segoe UI" />
        </Border>
        <Border Background="#CC1E1E28"
                CornerRadius="6"
                Padding="12,6"
                Canvas.Left="24"
                Canvas.Top="24">
            <TextBlock Foreground="#FFE8E8F0" FontSize="13" FontFamily="Segoe UI"
                       Text="Выделите область для OCR · Esc — отмена" />
        </Border>
    </Canvas>
</Window>
